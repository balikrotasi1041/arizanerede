import { deviceTypes,brands,families,models,issues,pathForDeviceType,pathForBrand,pathForFamily,pathForModel,pathForIssue } from "./data.js";
import { esc,ext,page,crumb,tree,SITE_ORIGIN } from "./ui-base.js";
export function renderHome(){const cards=deviceTypes.map(d=>`<a class="card cardLink" href="${pathForDeviceType(d)}"><span class="kicker">Cihaz türü</span><h3>${esc(d.name)}</h3><p>${esc(d.description)}</p><b>Markaları ve modelleri aç →</b></a>`).join("");return page("Arıza Nerede? | Cihaz arızası, hata kodu, kılavuz ve resmî servis","Cihaz türünü, markayı, seriyi ve modeli seç; hata kodunun anlamını, güvenli çözümü, kılavuzu, yazılım kaynağını ve resmî servisi bul.",`${SITE_ORIGIN}/`,`<div class="shell"><section class="hero"><span class="eyebrow">Teşhis ağacı + resmî kaynak</span><h1>Cihazın ne söylüyor? Arızanın izini adım adım bul.</h1><p>Marka, model, hata kodu, belirti veya kılavuzdaki bir ibareyi ara. Her teknik sorun için tek doğrulanmış cevap merkezi.</p><form class="search" action="/ara"><input name="q" maxlength="140" placeholder="Örn: Roborock S8 Error 10 ne demek?"><button class="btn">Ara</button></form></section><section class="section" id="cihazlar"><h2>Cihaz türünü seç</h2><div class="grid">${cards}</div></section></div>`)}
export function renderDeviceType(d){const rows=brands.filter(b=>b.deviceTypes.includes(d.slug)).map(b=>`<a class="card cardLink" href="${pathForBrand(d.slug,b.slug)}"><span class="kicker">${esc(d.name)}</span><h3>${esc(b.name)}</h3><p>Seri/model aileleri ve doğrulanmış arıza kayıtları.</p></a>`).join("");return page(`${d.name} arıza kodları ve modeller | Arıza Nerede?`,`${d.name} markaları, modelleri, hata kodları, kılavuz ve resmî servis kaynakları.`,`${SITE_ORIGIN}${pathForDeviceType(d)}`,`<div class="shell">${crumb([{label:"Ana sayfa",url:"/"},{label:d.name}])}<section class="detail"><span class="eyebrow">Cihaz türü</span><h1 class="title">${esc(d.name)}</h1><p class="lead">Önce markayı, sonra seri/model ailesini ve tam cihaz modelini seç.</p><div class="grid">${rows}</div></section></div>`)}
export function renderBrand(d,b){const rows=families.filter(f=>f.deviceType===d.slug&&f.brand===b.slug).map(f=>`<a class="card cardLink" href="${pathForFamily(f)}"><span class="kicker">Seri / model ailesi</span><h3>${esc(f.name)}</h3><p>Tam cihaz modellerini aç.</p></a>`).join("");return page(`${b.name} ${d.name} modelleri ve arızaları | Arıza Nerede?`,`${b.name} ${d.name} seri ve modelleri, hata kodları, kılavuz ve destek.`,`${SITE_ORIGIN}${pathForBrand(d.slug,b.slug)}`,`<div class="shell">${crumb([{label:"Ana sayfa",url:"/"},{label:d.name,url:pathForDeviceType(d)},{label:b.name}])}<section class="detail"><span class="eyebrow">${esc(d.name)}</span><h1 class="title">${esc(b.name)}</h1><div class="grid">${rows||'<div class="empty">Model ailesi hazırlanıyor.</div>'}</div></section></div>`)}
export function renderFamily(d,b,f){const rows=models.filter(m=>m.deviceType===d.slug&&m.brand===b.slug&&m.family===f.slug).map(m=>`<a class="card cardLink" href="${pathForModel(m)}"><span class="kicker">Tam cihaz modeli</span><h3>${esc(b.name)} ${esc(m.name)}</h3><p>Arıza kodları, belirtiler, kılavuz, yazılım ve servis.</p></a>`).join("");return page(`${b.name} ${f.name} modelleri | Arıza Nerede?`,`${b.name} ${f.name} tam model listesi ve doğrulanmış arıza kaynakları.`,`${SITE_ORIGIN}${pathForFamily(f)}`,`<div class="shell">${crumb([{label:"Ana sayfa",url:"/"},{label:d.name,url:pathForDeviceType(d)},{label:b.name,url:pathForBrand(d.slug,b.slug)},{label:f.name}])}<section class="detail"><span class="eyebrow">Seri / model ailesi</span><h1 class="title">${esc(b.name)} ${esc(f.name)}</h1><div class="grid">${rows||'<div class="empty">Tam model kayıtları hazırlanıyor.</div>'}</div></section></div>`)}
export function renderModel(d,b,f,m){const rows=issues.filter(i=>i.model===m.slug&&i.brand===b.slug&&i.deviceType===d.slug).map(i=>`<a class="result" href="${pathForIssue(i)}"><small>${esc(i.code)}</small><br><strong>${esc(i.title)}</strong><p>${esc(i.short)}</p></a>`).join("");const soft=m.softwareResources?.length?m.softwareResources.map(r=>`<div class="source"><small>Yazılım / firmware / uygulama</small>${ext(r.url,r.label)}<p>${esc(r.note)}</p></div>`).join(""):`<div class="source"><small>Yazılım</small>Doğrulanmış ayrı bir indirme kaynağı bulunmuyor.</div>`;return page(`${b.name} ${m.name} arıza kodları, kılavuz ve destek | Arıza Nerede?`,`${b.name} ${m.name} hata kodları, sorunlar, kullanıcı kılavuzu, resmî destek ve yazılım kaynağı.`,`${SITE_ORIGIN}${pathForModel(m)}`,`<div class="shell">${crumb([{label:"Ana sayfa",url:"/"},{label:d.name,url:pathForDeviceType(d)},{label:b.name,url:pathForBrand(d.slug,b.slug)},{label:f.name,url:pathForFamily(f)},{label:m.name}])}<section class="detail"><span class="eyebrow">Tam cihaz modeli</span><h1 class="title">${esc(b.name)} ${esc(m.name)}</h1>${tree([{label:d.name,url:pathForDeviceType(d)},{label:b.name,url:pathForBrand(d.slug,b.slug)},{label:f.name,url:pathForFamily(f)},{label:m.name}])}<div class="cols"><article><h2>Arıza kodları ve belirtiler</h2><div class="results">${rows||'<div class="empty">Bu model için doğrulanmış sorun kaydı hazırlanıyor.</div>'}</div></article><aside class="side"><div class="card"><span class="kicker">Resmî model kaynakları</span><div class="source">${ext(m.supportUrl,"Resmî destek")}</div><div class="source">${ext(m.manualUrl,"Kılavuz / belgeler")}</div>${soft}</div></aside></div></section></div>`)}
