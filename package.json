{
  "name": "arizanerede",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "validate:security": "node scripts/verify-security.mjs",
    "check": "node scripts/verify-data.mjs && npm run validate:security && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
